#include <stdio.h>
#include <math.h>

int main()
{
	
	
	if (flg != 0) {
		printf ("no solutions!\n");
		return EXIT_FAILURE;
	}

	printf ("x1 = %.2f x2 = %.2f\n", data->x1, data->x2);
	return EXIT_SUCCESS;
}
	return 0;
}
